apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: http-app-monitor
  namespace: keda-demo
spec:
  selector:
    matchLabels:
      app: http-app
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
