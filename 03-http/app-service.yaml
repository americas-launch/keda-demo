apiVersion: v1
kind: Service
metadata:
  name: http-app
  namespace: keda-demo
  labels:
    app: http-app
spec:
  selector:
    app: http-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
